services:
  - type: web
    name: helpdesk-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run report.py --server.port $PORT --server.address 0.0.0.0